spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/trade_engine_test
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.jdbc.time_zone: UTC
      hibernate.format_sql: true
  main:
    allow-bean-definition-overriding: true
